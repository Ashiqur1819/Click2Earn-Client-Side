

const BestWorkersCard = ({bestWorker}) => {

    const {name, email, photo, coins} = bestWorker || {}

    return (
      <div className="grid md:grid-cols-3 gap-6">
        <div>
          <div className="h-24 bg-red-300">dfdfd</div>
        </div>
      </div>
    );
};

export default BestWorkersCard;